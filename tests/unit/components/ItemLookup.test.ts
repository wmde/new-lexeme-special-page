describe( 'ItemLookup', () => {
	describe( ':props', () => {} );
	describe( '@events', () => {} );

	it.todo( 'searches for results on input' );
	it.todo( 'searches for more items if the menu is scrolled' );
} );
